using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AnimateTakeOffLanding : MonoBehaviour
{
    public enum _whichWay
    {
        TakeOff, Landing
    }
    public _whichWay whichWay = _whichWay.TakeOff;

    public bool startAnimation = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
